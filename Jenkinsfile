pipeline {
    agent any

    stages {
        stage('Preparation') { // for display purposes
        
    }
    stage('Build') {
        // Run the maven build
        
    }
    stage('Results') {
        
    }
        
    }
}